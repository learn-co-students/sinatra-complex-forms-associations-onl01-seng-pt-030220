<h1>Update Pet</h1>
Edits for <%= @pet.name %>

<form action="/pets/<%= @pet.id %>" method="POST">
    <input id="hidden" type="hidden" name="_method" value="patch">

Name: 
<input type="text" name="pet[name]" value="<%= @pet.name %>" id="pet_name">


Owner:
<% @owners.each do |owner|%>

    <input type="radio" 
    name="pet[owner_id]" 
    id="<%= owner.id %>" 
    value="<%= owner.id %>"
    <%= 'checked' if @pet.owner_id == owner.id %>>
    <%= owner.name %>
    </input>
    THIS US NOT SAVING THE NEW OWNER NOR SAVING UPDATED PET NAME NOR TICKING radio
    NOR LISTING ALL THE ACTUAL OWNERS - mine was missing

<%end%>

<br>
New Owner
<input type="text" name="owner[name]" id="owner_name">
</input>

<br>
<input type="submit" value="Update Pet">
</form>
